# 进行中事项交接（2026-09-14 凌晨暂停）

## 当前部署状态（无需任何操作即是最优）

- **主模型 best_s.pt（yolov10s）已上线**：BDD 路域 mAP50 **55.7**（v5=51.6 / v4=63.3混合口径），
  推理 14ms/帧，冒烟全绿。`python run.py` 直接可用。
- 灯色识别器已调参：真实数据 73.5% → **78.2%**（`core/traffic_light.py` 默认值已是调优后参数）。
- 全部实验结论见 `evaluation/metrics_report.md`。

## 明日待办 1：yolov10m 容量实验（已中止，未产生检查点）

昨晚跑到 epoch 1 的 32% 时暂停（速度 1.5 it/s 偏慢，预计全程 8 小时，故推迟）。
权重文件 `yolov10m.pt`（33.6MB，m 级已验证）已就位，**明天直接重跑**：

```bash
python train.py --data data_blindguard_v5.yaml --weights yolov10m.pt --epochs 6 --imgsz 640 --device 0 --workers 2 --batch 6 --name blindguard_m
```

- 预计 6-8 小时（夜间挂机最合适）；建议睡前启动
- 提速选项：epochs 6→4（约 5.5h），或白天插电+性能模式可缓解降频
- 完成后评估：`python -c "from ultralytics import YOLO; YOLO('runs/detect/blindguard_m/weights/best.pt').val(data='data_bdd_val.yaml', imgsz=640, workers=2)"`
- **对比基准**：v5 同口径 BDD 路域；m 若 ≥55 左右（追平 s@960）则换装
- 评估结论写入 `evaluation/metrics_report.md`

## 明日待办 2（可选）：真 s 系延长实验已证伪，勿重复

- 延长轮次（51.1 vs 51.6）与 POV 伪标注（48.9）均已证伪，详见评测报告，不要重跑。

## 长期事项（需要你本人）

1. 自采第一人称实拍数据（1-2 小时素材 + `evaluate.py collect`/`docs/重训练指南.md` 标注流程）
2. 视障用户实测（方案：`docs/用户测试方案.md`）
3. 井盖 1 万张数据集手动获取（CSDN，QQ767172261 仓库 README 指引）→ 并入训练替代辅助模型
