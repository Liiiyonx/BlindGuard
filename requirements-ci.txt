# Reproducible dependencies for CI and the lightweight unit-test suite.
# This intentionally excludes Ultralytics, Torch, and TTS packages so tests
# never download CUDA wheels or initialize a model runtime.
Flask==3.1.3
Werkzeug==3.1.8
opencv-python-headless==4.11.0.86
numpy==1.26.4
PyYAML==6.0.3
python-dotenv==1.2.2
